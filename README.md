# azogue.ai

One-page site for Azogue AI. Static HTML, no build step, no dependencies.

- `index.html` — the whole site
- `img/` — hero backgrounds, WebP with JPEG fallback (landscape for desktop, portrait for mobile)
- `CNAME` — custom domain for GitHub Pages

Edit `index.html` and push; Pages redeploys automatically.
